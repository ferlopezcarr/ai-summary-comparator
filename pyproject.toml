[project]
name = "ai-summary-comparator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bert-score>=0.3.13",
    "openai>=2.23.0",
    "rouge-score>=0.1.2",
]
